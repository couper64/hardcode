import setuptools as setuptools

setuptools.setup(
    name    = "setuptools-editable-setuppy",
    version = "0.0.1"                      ,
    install_requires = [ # Optional parameter.
        "tqdm == 4.64.1"
    ]
)